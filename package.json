{
  "main": "bindings/node",
  "scripts": {
    "build": "tree-sitter generate && node-gyp rebuild",
    "test": "tree-sitter test",
    "build-test": "tree-sitter generate && node-gyp rebuild && tree-sitter test"
  },
  "devDependencies": {
    "node-gyp": "^8.4.1",
    "tree-sitter-cli": "^0.20.1"
  },
  "dependencies": {
    "nan": "^2.15.0"
  }
}
